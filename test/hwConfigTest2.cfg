process read = {


     untracked PSet maxEvents = {untracked int32 input = 1}
     #source = EmptySource { }

     source = EmptySource{ }
   service = MessageLogger
    {
          untracked vstring destinations = {"log.txt"}
    #      
    #      untracked vstring categories = { "FwkJob" }
    #
    #      untracked PSet cout = 
    #      { 
    #         untracked PSet default = { untracked int32 limit = 0 }    # kill all messages in the log
    #         untracked PSet FwkJob  = { untracked int32 limit = -1 }  # except *all* of FwkJob's   
    #      }
          
    #untracked vstring fwkJobReports = {"FrameworkJobReport.xml"}
    }


include "CondCore/DBCommon/data/CondDBSetup.cfi"
  es_source = PoolDBESSource {
     using CondDBSetup
#     string connect = "frontier://FrontierDev/CMS_COND_RPC"
#     string connect = "oracle://cms_orcoff_int2r/CMS_COND_RPC"
     string connect = "sqlite_file:L1RPCHwConfig.db"
     VPSet toGet = {{ string record = "L1RPCHwConfigRcd" string tag = "L1RPCHwConfig_v1" }}
  }

     
     module tmf = TestHWConfig {}


     path p = { tmf }
}

